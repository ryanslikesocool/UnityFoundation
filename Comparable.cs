using System;

/*
namespace Foundation {
    public interface Comparable<Value> : IComparable<Value>, IEquatable<Value> where Value : IComparable<Value> {
        public static bool operator >(Comparable<Value> lhs, Comparable<Value> rhs) {
            return lhs.CompareTo(rhs) > 0;
        }

        public static bool operator <(Comparable<Value> lhs, Comparable<Value> rhs) {
            return lhs.CompareTo(rhs) > 0;
        }
    }
}
*/